# =============================================================================
# .gitignore — 告诉 Git 哪些文件/文件夹不需要纳入版本控制
#
# 列在这里的文件不会被 git add 提交到仓库。
# 这些通常是自动生成的、本地环境相关的、或者体积较大的文件，
# 每个人的机器上都不一样，提交了反而会造成冲突。
# =============================================================================

# Python 运行时自动生成的缓存文件，提交了只会污染仓库
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Python 虚拟环境目录，每个人本地自己创建就好
venv/
.venv/

# 程序运行时生成的数据文件（首次运行会自动创建）
data/

# pytest 运行后生成的缓存
.pytest_cache/

# IDE / 编辑器的配置文件，每个人用的编辑器和设置不同
.idea/          # PyCharm
.vscode/        # VS Code
*.swp           # Vim 临时文件
*.swo

# 操作系统自动生成的文件
.DS_Store       # macOS
Thumbs.db       # Windows
